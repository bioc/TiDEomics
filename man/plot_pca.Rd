% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_pca.R
\name{plot_pca}
\alias{plot_pca}
\title{Plot PCA}
\usage{
plot_pca(
  se_obj,
  plot = TRUE,
  plot_screeplot = TRUE,
  plot_loadings = TRUE,
  plot_morepc = TRUE,
  morepc = seq(1, 5),
  pc1 = 1,
  pc2 = 2,
  circle = FALSE,
  xlim_min = NULL,
  xlim_max = NULL,
  ylim_min = NULL,
  ylim_max = NULL,
  fontsize = 8,
  assay = 1,
  ...
)
}
\arguments{
\item{se_obj}{A SummarizedExperiment object created by \code{create_input()}}

\item{plot}{Logical, whether to plot PCA and other plots (default is TRUE)}

\item{plot_screeplot}{Logical, whether to plot screeplot with
\code{PCAtools::screeplot()} (default is TRUE)}

\item{plot_loadings}{Logical, whether to plot loadings with
\code{PCAtools::plotloadings()} (default is TRUE)}

\item{plot_morepc}{Logical, whether to plot pairs of more PCs with
\code{PCAtools::pairsplot()} (default is TRUE)}

\item{morepc}{A numeric vector of PCs to plot in the pairs plot
(default is 1:5)}

\item{pc1}{Numeric, which PC to use for the x-axis (default is 1)}

\item{pc2}{Numeric, which PC to use for the y-axis (default is 2)}

\item{circle}{Logical, whether to draw circles (ellipses) around samples
of each group (default is FALSE)}

\item{xlim_min}{Minimum x-axis limit when drawing ellipses
(default 1.5*min PC1)}

\item{xlim_max}{Maximum x-axis limit when drawing ellipses
(default 1.5*max PC1)}

\item{ylim_min}{Minimum y-axis limit when drawing ellipses
(default 1.5*min PC2)}

\item{ylim_max}{Maximum y-axis limit when drawing ellipses
(default 1.5*max PC2)}

\item{fontsize}{Font size for the PCA plot (default is 8)}

\item{assay}{Assay index to use, where 1 is the original data and 2 is
normalised to time 0 (if available) (default is 1)}

\item{...}{Additional arguments passed to \code{ggforce::geom_mark_ellipse()}
for customizing the ellipses}
}
\value{
A PCA plot showing the distribution of samples, other plots
provided by PCAtools package, and PCAtools output object for custom plotting.
}
\description{
Plot principal component analysis (PCA), using features
without missing values

The samples are coloured by Group and sized by Time. Ellipses are drawn
for each Group.
}
\examples{
data("example")
PC = plot_pca(example_obj, morepc = seq(1, 3))
}
