% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flatten_enrich.R
\name{flatten_enrich}
\alias{flatten_enrich}
\title{Flatten nested enrichment results}
\usage{
flatten_enrich(enrich_list)
}
\arguments{
\item{enrich_list}{Output of \code{\link[=enrichGO_list]{enrichGO_list()}}, \code{\link[=enrichR_list]{enrichR_list()}}, or
\code{\link[=enrich_msigdb]{enrich_msigdb()}}; or a named list combining several such outputs
(e.g. \code{list(GO = go_res, MSigDB = msigdb_res)}).}
}
\value{
A flat named list of data frames or \code{enrichResult} objects
with \code{DeeDeeExperiment}-compatible names and columns
(e.g. "clusterProfiler_BP_1", "enrichr_DSigDB").
Returns an empty list if \code{enrich_list} is empty.
}
\description{
Flattens the nested enrichment result structure returned by
\code{\link[=enrichGO_list]{enrichGO_list()}}, \code{\link[=enrichR_list]{enrichR_list()}}, or \code{\link[=enrich_msigdb]{enrich_msigdb()}} into a
flat named list of data frames or \code{enrichResult} objects.
This is useful for exporting results to other tools such as
\code{DeeDeeExperiment}.
}
\examples{
data(example_go)
enrich_flat <- flatten_enrich(example_go)
str(enrich_flat, max.level = 1)
}
