% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_modules_v.R
\name{plot_modules_v}
\alias{plot_modules_v}
\title{Plot modules (vertical layout)}
\usage{
plot_modules_v(
  module,
  se_obj_merged,
  scale = TRUE,
  assay = 2,
  ylabel = "Log2 abundance normalised to Time 0",
  suffix = "",
  device = "png",
  save = NULL,
  width = 12,
  height = 8,
  height_ratio = 2,
  fontsize = 8,
  res = 300
)
}
\arguments{
\item{module}{A data frame with columns "Feature" and "Module"}

\item{se_obj_merged}{A SummarizedExperiment object, with one value for each
feature at each time point in each group (replicates merged). The colData of
the object should contain columns "Sample", "Group", and "Time". The object
can be produced by \code{split_groups()}, \code{merge_replicates()} and
\code{merge_groups()}.}

\item{scale}{Whether to scale the data (z-score) across samples for each
feature (default is TRUE)}

\item{assay}{The assay index in the SummarizedExperiment object to use
(default is 2, time 0 normalised data)}

\item{ylabel}{Y axis label prefix (default is "Abundance")}

\item{suffix}{Suffix for the saved image file name (default is an empty
string)}

\item{device}{Image file format(s) for saving. Can be a character
vector, e.g. \code{c("png", "pdf")}, to save in multiple formats
(default: \code{"png"}).}

\item{save}{Directory to save the plot, no saving if is NULL
(default is NULL)}

\item{width}{Width of the saved image (default is 12 (cm))}

\item{height}{Height of the saved image (default is 8 (cm))}

\item{height_ratio}{Ratio of height of heatmap to the line plot
(default is 2)}

\item{fontsize}{Font size (default is 8)}

\item{res}{Resolution of the saved image (except pdf format)
(default is 300 (ppi))}
}
\value{
Two plots aligned vertically by groups: the top one is a line plot
of module feature mean expression profiles, the bottom one is a heatmap of
feature expression across time points.
}
\description{
Plot WGCNA modules' mean expression profiles and heatmaps,
align vertically.

Different from running WGCNA, the input data should have the
replicates merged, instead of having multiple samples per group, time and
feature (gene).

If certain time points are missing in some groups, NA values are added.
}
\examples{
library(magrittr)
data(example)
example_obj <- normalise_to_start(example_obj)
example_obj_list <- split_groups(example_obj)
example_obj_merged_list <- merge_replicates(example_obj_list)
example_obj_merged <- merge_groups(example_obj_merged_list)

data(example_net)
example_module <- WGCNA_module(example_net) 

plot_modules_v(example_module \%>\% dplyr::filter(Module != '0'),
    example_obj_merged, scale = TRUE,
    ylabel = "Z-score of log2 (expression)",
    height_ratio = 2,
    fontsize = 6)
}
