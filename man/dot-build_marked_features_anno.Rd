% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_modules_h.R
\name{.build_marked_features_anno}
\alias{.build_marked_features_anno}
\title{Build marked-feature term_list for multi-column textbox}
\usage{
.build_marked_features_anno(
  mark_features,
  module_df,
  mark_state,
  fontsize,
  mod_levels
)
}
\arguments{
\item{mark_features}{A named list of character vectors, mapping
category names to feature IDs, or a plain character vector of
feature IDs (all shown in black).}

\item{module_df}{A data frame with columns \code{Feature} and \code{Module}.}

\item{mark_state}{A list with element \code{feat_col}, a named character
vector mapping feature IDs to colours.}

\item{fontsize}{Numeric font size for term text.}

\item{mod_levels}{Character vector of module levels to include.}
}
\value{
A named list of per-module data.frames (columns \code{text},
\code{col}, \code{fontsize}), or \code{NULL} if no marked features are found in
the module data.
}
\description{
Build a per-module term_list from \code{mark_features} for use as a column
in \code{.anno_multicol_textbox}. Accepts a named list (per-category colours
from \code{ggsci::pal_jco()}) or a character vector (all black). Colours
are taken from \code{mark_state$feat_col}.
}
\keyword{internal}
