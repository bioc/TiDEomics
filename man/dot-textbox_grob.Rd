% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/anno_multicol_textbox.R
\name{.textbox_grob}
\alias{.textbox_grob}
\title{Local textbox grob}
\usage{
.textbox_grob(
  text,
  x = unit(0.5, "npc"),
  y = unit(0.5, "npc"),
  just = "centre",
  gp = grid::gpar(),
  background_gp = grid::gpar(col = "transparent", fill = "transparent"),
  round_corners = FALSE,
  r = unit(0.1, "snpc"),
  line_space = unit(4, "pt"),
  text_space = unit(4, "pt"),
  max_width = unit(100, "mm"),
  padding = unit(4, "pt"),
  first_text_from = "top",
  add_new_line = FALSE,
  word_wrap = FALSE
)
}
\arguments{
\item{text}{Character vector of text strings to display.}

\item{x}{X position as a \code{grid::unit} (default: 0.5 npc).}

\item{y}{Y position as a \code{grid::unit} (default: 0.5 npc).}

\item{just}{Justification of the viewport (default: \code{"centre"}).}

\item{gp}{A \code{grid::gpar} object with graphical parameters for text.
Elements \code{col}, \code{fontsize}, \code{fontfamily}, and \code{fontface} are
recycled to match \code{length(text)}.}

\item{background_gp}{A \code{grid::gpar} object for the background rectangle
(default: transparent fill and border).}

\item{round_corners}{Logical; whether to draw rounded corners on the
background rectangle (default: \code{FALSE}).}

\item{r}{Corner radius as a \code{grid::unit} when \code{round_corners = TRUE}
(default: 0.1 snpc).}

\item{line_space}{Spacing between lines as a \code{grid::unit}
(default: 4 pt).}

\item{text_space}{Spacing between inline text segments as a
\code{grid::unit} (default: 4 pt).}

\item{max_width}{Maximum width as a \code{grid::unit}
(default: 100 mm).}

\item{padding}{Padding around content as a \code{grid::unit} vector of
length 1, 2, or 4 (default: 4 pt all sides).}

\item{first_text_from}{\code{"top"} (default) or \code{"bottom"}.}

\item{add_new_line}{Logical; force each text string to a new line
(default: \code{FALSE}).}

\item{word_wrap}{Logical; enable word wrapping (default: \code{FALSE}).}
}
\value{
A \code{grid::gTree} of class \code{"textbox"}.
}
\description{
Stacks text strings vertically with per-string graphical parameters.
Adapted from \code{ComplexHeatmap:::textbox_grob} to avoid dependency on
unexported ComplexHeatmap internals.
}
\keyword{internal}
