% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_WGCNA.R
\name{run_WGCNA}
\alias{run_WGCNA}
\title{Weighted gene co-expression network analysis}
\usage{
run_WGCNA(wgcna_input, power, numericLabels = TRUE, ...)
}
\arguments{
\item{wgcna_input}{A list output by \code{prepare_WGCNA()}, containing the
prepared input data and networkType parameter used in power selection.}

\item{power}{Soft-thresholding power to be used in
\code{WGCNA::blockwiseModules()}, selected automatically or manually based
on the output of \code{prepare_WGCNA()}}

\item{numericLabels}{Whether to use numeric labels for modules in the output
(default is TRUE)}

\item{...}{Additional parameters to be passed to \code{WGCNA::blockwiseModules()}}
}
\value{
The built network and parameters of \code{WGCNA::blockwiseModules()},
and the input data and sample information for use in \code{plot_WGCNA()}.
}
\description{
Run WGCNA based on output of \code{prepare_WGCNA()}, to
identify co-expression modules of features with \code{WGCNA::blockwiseModules()}
}
\examples{
data("example")
example_obj <- normalise_to_start(example_obj)

wgcna_input <- prepare_WGCNA(example_obj, assay = 2, powers = seq(1, 30),
    networkType = "signed", RsquaredCut = 0.8)
wgcna_input$fitIndices
picked_power <- wgcna_input$powerEstimate
example_net <- run_WGCNA(wgcna_input,
    power = picked_power,
    minModuleSize = 10, # only 100 genes in the example data
    numericLabels = TRUE)
}
\references{
https://github.com/edo98811/WGCNA_official_documentation/blob/main/FemaleLiver-03-relateModsToExt.R
}
