% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/anno_multicol_textbox.R
\name{.anno_multicol_textbox}
\alias{.anno_multicol_textbox}
\title{Multi-column textbox row annotation for ComplexHeatmap}
\usage{
.anno_multicol_textbox(
  align_to,
  text,
  background_gp = grid::gpar(fill = "#DDDDDD", col = "#AAAAAA"),
  which = c("row", "column"),
  by = "anno_link",
  side = c("right", "left"),
  ...
)
}
\arguments{
\item{align_to}{A vector of group labels, a list of indices, or a
named list mapping groups to row indices (same as
\code{ComplexHeatmap::anno_textbox}).}

\item{text}{A list of per-module enrichment terms, where each element
is a named list of category term data.frames (columns \code{text}, \code{col},
\code{fontsize}). Output from \code{.reformat_cat_terms}.}

\item{background_gp}{Background graphical parameters for the annotation
block (fill, col, lty, lwd).}

\item{which}{\code{"row"} (only row annotations are supported).}

\item{by}{Link type: \code{"anno_link"} (default, aligns to heatmap rows)
or \code{"anno_block"}.}

\item{side}{\code{"right"} (default) or \code{"left"}.}

\item{...}{Additional arguments passed to \code{.multicol_textbox_grob}.
Notable options: \code{show_headers} (logical), \code{header_gp} (gpar for
header text), \code{header_offset} (unit offset above annotation).}
}
\value{
A ComplexHeatmap annotation object (from \code{anno_link} or
\code{anno_block}).
}
\description{
Row annotation that displays multiple enrichment categories side by side
in a single annotation block. Follows the same interface convention as
\code{ComplexHeatmap:::anno_textbox}. Column headers are drawn above the
first module via \code{decorate_annotation}-style header placement.
}
\keyword{internal}
