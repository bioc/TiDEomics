% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare_tide.R
\name{prepare_tide}
\alias{prepare_tide}
\title{Prepare TiDEomics input}
\usage{
prepare_tide(
  data,
  sample_ann,
  subject_col = NULL,
  replicate_col = NULL,
  batch_col = NULL,
  feature_threshold = 0,
  filter_ratio = 0.5,
  min_groups = 2,
  keep = c("below_quantile", "top_n", "threshold"),
  residual_threshold = NULL,
  n_keep = 5000,
  interaction = FALSE,
  n_cores = 1
)
}
\arguments{
\item{data}{A data frame with rows as features (e.g., genes, proteins)
and columns as samples. The first column should contain feature identifiers
(e.g., gene symbols) and be named 'Feature'. The data should have been
normalised and log transformed. Missing values are allowed.}

\item{sample_ann}{A data frame containing sample annotations with required
columns: 'Sample', 'Group', and 'Time'. 'Replicate' and 'Batch' are optional
columns and will be auto-generated if not provided. 'Time',
'Replicate' and 'Batch' should be numeric.}

\item{subject_col}{Optional: name of a column in \code{sample_ann} identifying
biological subjects measured repeatedly across time points (e.g.,
\code{"PatientID"}). If provided, the column is renamed to 'Subject' and used
for repeated-measures analyses downstream. If NULL (default), all samples
are treated as independent, e.g. for cell culture experiments.}

\item{replicate_col}{Optional: name of a column in \code{sample_ann} identifying
replicate IDs. If provided, the column is renamed to 'Replicate'. If NULL
(default), the function looks for a column named 'Replicate'; if absent,
replicate IDs are auto-generated within each Group and Time (and Subject,
if provided).}

\item{batch_col}{Optional: name of a column in \code{sample_ann} identifying
batch information. If provided, the column is renamed to 'Batch'. If NULL
(default), the function looks for a column named 'Batch'; if absent, all
samples are assigned to batch 1.}

\item{feature_threshold}{Threshold for \code{calc_feature_property()}. Feature
values > feature_threshold are considered expressed (default: 0).}

\item{filter_ratio}{Minimum ratio of time points a feature must be expressed
in, to be considered expressed in a group (default: 0.5, meaning that a
feature must be > feature_threshold in >=50\% time points in a group to be
considered expressed). Passed to \code{group_specific_features()}.}

\item{min_groups}{Minimum number of groups a feature must be expressed in,
with the above filter_ratio, to pass the cross group filter
(default: 2). Passed to \code{group_specific_features()}.}

\item{keep}{How to select features after variance decomposition:
\code{"below_quantile"} (default): keep features with residual variance below
\code{residual_threshold} quantile (0-1 scale).
\code{"top_n"}: keep the top \code{n_keep} lowest-residual features.
\code{"threshold"}: keep features with residual percentage below
\code{residual_threshold} (0-100 scale; use 100 to skip filtering).}

\item{residual_threshold}{Numeric: when \code{keep = "below_quantile"},
the quantile of residual variance to use as cutoff (0-1 scale).
When \code{keep = "threshold"}, the percentage cutoff
(0-100 scale, 100 = keep all features).
(default: NULL, auto-assigned as 0.75 for "below_quantile",
100 for "threshold")}

\item{n_keep}{Number of features to keep when \code{keep = "top_n"}
(default: 5000).}

\item{interaction}{Passed to \code{decomp_variance()}: if TRUE, adds
\code{(1|Group:Time)} (or
\code{(1|Subject:Time)} when Subject present) interaction term (default: FALSE).}

\item{n_cores}{Number of cores for variance decomposition (default: 1).}
}
\value{
A named list with elements. The elements suffixed with
"_filtered" have passed both the cross-group and residual filters,
and are split into $orig and $norm sub-lists,
with variance decomposition and residual filtering done per assay.
\enumerate{
\item \code{se} (SE with replicates, all features, both assays);
\item \code{se_filtered} (a list of two SEs with replicates,
filtered features by \code{orig} / \code{norm}, use for WGCNA);
\item \code{merged_list} (per-group merged SEs, all features, use for Trendy);
\item \code{merged_list_filtered} (a list of two per-group merged SEs,
filtered features by \code{orig} / \code{norm});
\item \code{merged_se} (single merged SE, all groups combined, all features,
use for plot_modules_v/h);
\item \code{merged_se_filtered} (a list of two single merged SEs, filtered features
by \code{orig} / \code{norm}, use for plot_modules_v/h);
\item \code{variance} (a list of two variance decomposition data.frames,
computed from \code{orig} / \code{norm} assay);
\item \code{feature_property} (feature property summary, all features);
\item \code{filter_summary} (per-stage filtering statistics);
\item \code{group_specific_filter} (a vector of group-specific features,
removed by the cross-group filter);
11-13. \code{DE}, \code{enrichment}, \code{WGCNA} (empty on initialization).
}
}
\description{
One-step data preparation wrapper for TiDEomics: creates the
SummarizedExperiment object, normalises to starting time point,
filters out group-specific features, runs variance
decomposition, and filters out noisy features with high residual
variance. Returns both unmerged and merged
(mean of replicates) outputs, with all-feature and filtered
versions of each, ready for downstream analysis.
}
\details{
The pipeline:
\enumerate{
\item \code{create_input()}: validate and build SE
\item \code{normalise_to_start()}: add time-0-normalised assay
\item \code{split_groups()} -> \code{merge_replicates()} -> \code{calc_feature_property()}
-> \code{summarise_feature_property()}: per-feature expression statistics
\item \code{group_specific_features()}: filter out group-specific features
\item \code{decomp_variance()} : LMM variance decomposition on both \code{"orig"}
and \code{"norm"} assays
\item Residual filter: filter out features with high residual
variance, calculated from \code{decomp_variance()} on each assay
\item Produce filtered-unmerged and filtered-merged SEs for each assay
}
}
\examples{
data(tutorial_data)
data(tutorial_sample_info)
tide <- prepare_tide(tutorial_data, tutorial_sample_info,
    keep = "threshold", residual_threshold = 100)
tide$filter_summary
}
