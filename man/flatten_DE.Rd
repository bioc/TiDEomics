% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flatten_DE.R
\name{flatten_DE}
\alias{flatten_DE}
\title{Flatten nested differential expression results}
\usage{
flatten_DE(de_list)
}
\arguments{
\item{de_list}{A list of DE results. Accepts individual output of
\code{\link[=DE_between_group]{DE_between_group()}} or \code{\link[=DE_between_time]{DE_between_time()}} with an \code{all_list}
element; or a named list (e.g. \code{list(between_group = ..., between_time = ...)}), each containing an \code{all_list} element.}
}
\value{
A named list of data frames with columns renamed for
\code{DeeDeeExperiment} compatibility (\code{log2FoldChange}, \code{pvalue}, \code{padj}).
Returns an empty list if \code{de_list} is empty.
}
\description{
Flattens the nested DE result structure returned by
\code{\link[=DE_between_group]{DE_between_group()}} or \code{\link[=DE_between_time]{DE_between_time()}} into a named list of
data frames, one entry per contrast-time combination.
This is useful for exporting results to other tools such as
\code{DeeDeeExperiment}.
}
\examples{
data(tutorial_data)
data(tutorial_sample_info)
tide <- prepare_tide(tutorial_data, tutorial_sample_info,
    keep = "threshold", residual_threshold = 100)
tide$DE <- DE_between_group(tide$se, assay = "norm",
    filter = 1, trend = TRUE)
de_flat <- flatten_DE(tide$DE)
str(de_flat, max.level = 1)
}
