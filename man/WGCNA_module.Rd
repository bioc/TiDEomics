% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/WGCNA_module.R
\name{WGCNA_module}
\alias{WGCNA_module}
\title{Convert WGCNA output to feature-module data frame}
\usage{
WGCNA_module(net, exclude_grey = FALSE)
}
\arguments{
\item{net}{The output of \code{run_WGCNA()} (a list containing \verb{$colors})}

\item{exclude_grey}{Logical. If \code{TRUE}, features assigned to
module \code{0} (grey / unassigned) are removed. Default: FALSE}
}
\value{
A data.frame with columns \code{Feature} (character) and \code{Module}
(factor ordered by decreasing module size). When \code{exclude_grey = TRUE},
grey/unassigned features are excluded.
}
\description{
Converts the module assignments from \code{run_WGCNA()} output
into two-column data.frame with \code{Feature} and \code{Module} columns,
expected by \code{plot_modules_v()},
\code{plot_modules_h()}, \code{summarise_module_pattern()}, and all enrichment
functions.
}
\examples{
data(example_net)
module <- WGCNA_module(example_net)
}
