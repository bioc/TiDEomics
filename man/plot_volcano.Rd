% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_volcano.R
\name{plot_volcano}
\alias{plot_volcano}
\title{Volcano plot of DE results}
\usage{
plot_volcano(
  DE_out,
  group1,
  group2,
  time,
  group,
  time1,
  time2,
  logFC_thres = NULL,
  adjP_thres = NULL,
  label = FALSE,
  fontsize = 8,
  ...
)
}
\arguments{
\item{DE_out}{Output from \code{DE_between_group()} or \code{DE_between_time()}}

\item{group1}{(Required for \code{DE_between_group()} output)
Name of the first group for comparison}

\item{group2}{(Required for \code{DE_between_group()} output)
Name of the second group for comparison}

\item{time}{(Required for \code{DE_between_group()} output)
Time point for comparison}

\item{group}{(Required for \code{DE_between_time()} output)
Name of the group for comparison}

\item{time1}{(Required for \code{DE_between_time()} output)
First time point for comparison}

\item{time2}{(Required for \code{DE_between_time()} output)
Second time point for comparison}

\item{logFC_thres}{(Optional) Log2 fold change threshold for
highlighting DE features, default is NULL (using threshold when
\code{DE_between_group()} or \code{DE_between_time()} was run)}

\item{adjP_thres}{(Optional) Adjusted p-value threshold for
highlighting DE features, default is NULL (using threshold when
\code{DE_between_group()} or \code{DE_between_time()} was run)}

\item{label}{(Optional) Whether to label names of DE features on the plot
(default is FALSE)}

\item{fontsize}{(Optional) Font size for the plot (default is 8)}

\item{...}{Additional arguments for ggrepel::geom_text_repel()
when label = TRUE}
}
\value{
A volcano plot of DE results
}
\description{
This function creates a volcano plot to visualize
the results of differential expression (DE) analysis performed by
\code{DE_between_group()} or \code{DE_between_time()}. The plot displays
log2 fold change on the x-axis and -log10 adjusted p-value on the
y-axis, with DE features highlighted based on specified thresholds.
If no thresholds are provided, the function will use the thresholds that
were applied when running \code{DE_between_group()} or \code{DE_between_time()}.
}
\examples{
data(example_obj)
example_obj <- normalise_to_start(example_obj)

DE_between_group_out <- DE_between_group(example_obj, assay = 2)
plot_volcano(DE_between_group_out, group1 = "untreated",
    group2 = "IFNbeta", time = 24,
    logFC_thres = 0.5, adjP_thres = 0.05, label = TRUE)
}
