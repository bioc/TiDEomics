% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare_WGCNA.R
\name{prepare_WGCNA}
\alias{prepare_WGCNA}
\title{Prepare data and choose power for WGCNA}
\usage{
prepare_WGCNA(
  se_obj,
  assay = 2,
  networkType = "signed",
  RsquaredCut = 0.8,
  MeanConnectivity = 100,
  powers = NULL,
  ...
)
}
\arguments{
\item{se_obj}{A SummarizedExperiment object. Data
normalised to time point 0 can be in the second assay slot, created by
\code{normalise_to_start()}. Features can be pre-filtered, e.g. by residual
variance calculated by \code{decomp_variance()}, to remove noisy features
before running WGCNA.}

\item{assay}{Which assay slot of the SummarizedExperiment object to use
for WGCNA input (default is 2, which is where the time 0 normalised data is
stored by \code{normalise_to_start()})}

\item{networkType}{(Optional) Parameter of \code{WGCNA::pickSoftThreshold()}
(default is "signed")}

\item{RsquaredCut}{(Optional) Parameter of \code{WGCNA::pickSoftThreshold()}
(default is 0.8)}

\item{MeanConnectivity}{(Optional) Line of mean connectivity
(default is 100)}

\item{powers}{(Optional) Parameter of \code{WGCNA::pickSoftThreshold()}
(default is \code{c(seq(1, 10, by = 1), seq(12, 20, by = 2))})}

\item{...}{Additional parameters to be passed to \code{WGCNA::pickSoftThreshold()}}
}
\value{
A list containing results of the scale-free topology
fit indices with different powers, suggested power, network type and prepared
input data used for reuse in \code{run_WGCNA()}
}
\description{
Prepare input data for WGCNA (format, QC),
then choose the appropriate soft thresholding power for
network construction, by analysing scale free topology
with different soft thresholding powers.
}
\examples{
data("example")
example_obj <- normalise_to_start(example_obj)

wgcna_input <- prepare_WGCNA(example_obj, assay = 2, powers = seq(1, 30),
    networkType = "signed", RsquaredCut = 0.8)
wgcna_input$fitIndices
picked_power <- wgcna_input$powerEstimate
# example_net <- run_WGCNA(wgcna_input,
#    power = picked_power,
#    minModuleSize = 10, # only 100 genes in the example data
#    numericLabels = TRUE)
# plot_WGCNA(example_net, fontsize = 8)
}
\references{
https://github.com/edo98811/WGCNA_official_documentation/
}
