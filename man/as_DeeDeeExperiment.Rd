% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as_DeeDeeExperiment.R
\name{as_DeeDeeExperiment}
\alias{as_DeeDeeExperiment}
\title{Convert a TiDEomics result to DeeDeeExperiment}
\usage{
as_DeeDeeExperiment(tide)
}
\arguments{
\item{tide}{A named list returned by \code{prepare_tide()}.}
}
\value{
A \code{DeeDeeExperiment} object if the \code{DeeDeeExperiment}
package is available, otherwise an error.
}
\description{
Wraps the output of \code{prepare_tide()} into a
\code{DeeDeeExperiment} object.
}
\examples{
data(tutorial_data)
data(tutorial_sample_info)
tide <- prepare_tide(tutorial_data, tutorial_sample_info,
    keep = "threshold", residual_threshold = 100)
if (requireNamespace("DeeDeeExperiment", quietly = TRUE)) {
    dde <- as_DeeDeeExperiment(tide)
}
}
