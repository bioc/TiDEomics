% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_DE_between_group.R
\name{plot_DE_between_group}
\alias{plot_DE_between_group}
\title{DE number between groups}
\usage{
plot_DE_between_group(
  DE_between_group_out,
  group = NULL,
  fontsize = 8,
  adjP_thres = NULL,
  logFC_thres = NULL
)
}
\arguments{
\item{DE_between_group_out}{Output of \code{DE_between_group()}, a list with
\code{all_list}, \code{de_list}, \code{ref_groups}, \code{all_groups} elements.}

\item{group}{A character vector specifying which groups to use
as the reference (Cond1) for plotting. If NULL, all groups in the output
will be plotted. (default is NULL)}

\item{fontsize}{Font size for the plot (default is 8)}

\item{adjP_thres}{Threshold for adjusted p-value to consider a
feature as differentially expressed, for re-filtering
the DE features. (default is NULL, no re-filtering)}

\item{logFC_thres}{Threshold for log2 fold change to consider a
feature as differentially expressed, for re-filtering
the DE features. (default is NULL, no re-filtering)}
}
\value{
A series of line plots showing the number of DE features over time
for each group comparison.
}
\description{
Plot the number of differentially expressed features between
groups over time, using the output of \code{DE_between_group()}.

DE features were pre-filtered with \code{DE_between_group()}, but by specifying
\code{adjP_thres} and \code{logFC_thres}, users can re-filter the DE features for
plotting.
}
\examples{
data(example_obj)
example_obj <- normalise_to_start(example_obj)

DE_between_group_out <- DE_between_group(example_obj, assay = 2)
plot_DE_between_group(DE_between_group_out, fontsize = 8)
}
