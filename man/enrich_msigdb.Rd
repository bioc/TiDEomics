% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrich_msigdb.R
\name{enrich_msigdb}
\alias{enrich_msigdb}
\title{Gene set enrichment via MSigDB}
\usage{
enrich_msigdb(
  gene_list,
  universe,
  category = NULL,
  subcategory = NULL,
  gene_sets = NULL,
  species = "Homo sapiens",
  db_species = "HS",
  name = NULL,
  pvalueCutoff = 0.05,
  pAdjustMethod = "BH",
  minGSSize = 10,
  maxGSSize = 500
)
}
\arguments{
\item{gene_list}{A named list of gene vectors, or a data.frame
with \code{Feature} and \code{Module} columns from \code{WGCNA_module()}.}

\item{universe}{Background genes (required).}

\item{category}{MSigDB category (default: \code{NULL}).
See \code{msigdbr::msigdbr_collections(db_species = "HS"/"MM")}
for available options.
Not required when \code{gene_sets} is provided; in that case all
collections are searched to locate the named gene sets.}

\item{subcategory}{MSigDB subcategory (default: \code{NULL}).
Set to NULL to include all subcategories of \code{category}.}

\item{gene_sets}{Optional character vector of specific MSigDB gene set
names to include (e.g., \code{c("HALLMARK_APOPTOSIS", "HALLMARK_P53_PATHWAY")}). When provided, \code{msigdbr} is queried across
all collections to find these gene sets, so \code{category} and
\code{subcategory} are not required. If NULL (default), all gene sets in
the category/subcategory are used.}

\item{species}{Species of input genes (default: \code{"Homo sapiens"}).
See \code{msigdbr::msigdbr_species()} for available options.}

\item{db_species}{Species of MSigDB genesets (default: \code{"HS"}).
Use "MM" for mouse-native gene sets. If species and db_species mismatch,
\code{db_species} MSigDB will be used with ortholog mapping to \code{species}.}

\item{name}{Name for the output list element. If NULL, auto-derived
from \code{category} (e.g., \code{"C2"} names the output element \code{"C2"}). If
\code{category} is also NULL, it uses \code{"msigdb"}.}

\item{pvalueCutoff}{Adjusted p-value cutoff (default: 0.05).}

\item{pAdjustMethod}{P-value adjustment method (default: \code{"BH"}).}

\item{minGSSize}{Minimum gene set size (default: 10).}

\item{maxGSSize}{Maximum gene set size (default: 500).}
}
\value{
A named list with one element: a data.frame with columns
\code{Cluster}, \code{ID}, \code{Description}, \code{GeneRatio}, \code{BgRatio}, \code{pvalue},
\code{p.adjust}, \code{Count}, \code{geneID}.
}
\description{
Enrichment analysis against MSigDB gene sets via
hypergeometric test. Supports MSigDB categories, subcategories,
and specified gene sets. Requires the \code{msigdbr} package.

Use cases:
\itemize{
\item Drug perturbations: \verb{category = "C2", subcategory = "CGP"}
(chemical and genetic perturbations)
\item TF targets: \verb{category = "C3", subcategory = "TFT:GTRD"}
\item Hallmarks: \verb{category = "H", subcategory = NULL} (no subcategory)
\item GO: \verb{category = "C5", subcategory = "BP"} (GO biological process)
}

Multiple testing: P-values are adjusted (Benjamini-Hochberg)
per module across the gene-set tests within that module.
}
\examples{
if (requireNamespace("msigdbr", quietly = TRUE)) {
    data(example_net)
    example_module <- WGCNA_module(example_net) |>
        dplyr::filter(Module \%in\% c("1", "2"))
    hallmark_msigdb <- enrich_msigdb(example_module, category = "MH",
        species = "Mus musculus", db_species = "MM",
        minGSSize = 1,
        pvalueCutoff = 0.9,
        universe = WGCNA_module(example_net, exclude_grey = FALSE)$Feature)
}
}
