% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrich_drug_list.R
\name{enrich_drug_list}
\alias{enrich_drug_list}
\title{Enrich for drug targets}
\usage{
enrich_drug_list(
  gene_list,
  drug_dbs = c("DSigDB", "DrugMatrix"),
  universe = NULL,
  universe_list = NULL,
  pvalueCutoff = 0.05
)
}
\arguments{
\item{gene_list}{A list of gene vectors, need to be gene symbols}

\item{drug_dbs}{A character vector of drug-related databases in enrichR
to use for enrichment analysis. Available databases can be checked with
\code{enrichR::listEnrichrDbs()}. (default is c("DSigDB", "DrugMatrix"))}

\item{universe}{Background genes for all input gene sets,
used if \code{universe_list} is not provided}

\item{universe_list}{Background genes for each input gene set,
a list of gene vectors with the same names as \code{gene_list},
need to be gene symbols.}

\item{pvalueCutoff}{(Optional) Adjusted p-value cutoff for filtering
enriched terms (default is 0.05)}
}
\value{
A data frame of enriched drug targets for each gene set in the
input list.
}
\description{
Enrichment analysis for drug targets of multiple gene sets
using enrichR, allowing for using different or same background genes for
each gene set
}
\examples{
library(magrittr)
data(example_net)
example_module <- data.frame(Module = as.factor(example_net$colors)) \%>\%
    tibble::rownames_to_column("Feature") \%>\% dplyr::arrange(Module)
example_module_list <- example_module \%>\% dplyr::filter(Module != 0) \%>\%
    split(as.character(.$Module)) \%>\%
    lapply(`[[`, "Feature")
# drugs_tb = enrich_drug_list(example_module_list, drug_dbs = c("DSigDB"),
#     universe = example_module$Feature, pvalueCutoff = 0.1)
}
