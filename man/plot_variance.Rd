% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_variance.R
\name{plot_variance}
\alias{plot_variance}
\title{Plot variance decomposition}
\usage{
plot_variance(
  var_decomp,
  rank = c("Group", "Time"),
  features = NULL,
  top_n = 20,
  show_ylab = TRUE,
  fontsize = 8
)
}
\arguments{
\item{var_decomp}{A data frame of variance decomposition result from
\code{variance_decomp()} function}

\item{rank}{Rank the plot by "Group" or "Time"}

\item{features}{Features to be plotted (default is NULL)}

\item{top_n}{Top n features ranked by \code{rank} to be plotted when \code{features}
is not specified (default is 20)}

\item{show_ylab}{Whether to show y axis text (default is TRUE)}

\item{fontsize}{Font size for the plot (default is 8)}
}
\value{
A plot of variance decomposition for each feature, showing the
percentage of variance explained by Group, Time, and Residual. The features
are ranked by the specified \code{rank} variable (Group or Time) and only the
top n features are plotted if \code{features} is not specified.
}
\description{
Based on \code{PALMO::variancefeaturePlot()} function
}
\examples{
data("example")
example_obj <- normalise_to_start(example_obj)

var_decomp <- decomp_variance(example_obj, assay = 1)
plot_variance(var_decomp, rank = "Time", top_n = 20)
}
