% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_Trendy.R
\name{.run_Trendy_one_group}
\alias{.run_Trendy_one_group}
\title{Segmented regression analysis (for one group)}
\usage{
.run_Trendy_one_group(
  se_obj_imp,
  minExp = 0.5,
  feature = NULL,
  maxK = 1,
  meanCut = 0,
  minNumInSeg = 3,
  NCores = 2,
  ...
)
}
\arguments{
\item{se_obj_imp}{A SummarizedExperiment object with no missing values}

\item{minExp}{Minimum expression ratio for a feature to be included in
the analysis (default is 0.5)}

\item{feature}{A character vector of feature names to be included in the
analysis. If NULL, all features with expression ratio (Exp_ratio) >= minExp
will be
used, based on results of \code{calc_feature_property()} before imputation.
(default is NULL)}

\item{maxK}{Parameter of \code{Trendy::trendy()}, maximum number of breakpoints
allowed in the segmented regression model (default is 1)}

\item{meanCut}{Parameter of \code{Trendy::trendy()}, minimum mean expression
required for a feature to be included in the analysis (default is 0)}

\item{minNumInSeg}{Parameter of \code{Trendy::trendy()}, minimum number of
samples required in each segment (default is 3)}

\item{NCores}{Number of cores to use for parallel processing (default is 2)}

\item{...}{Additional arguments to be passed to the \code{Trendy::trendy()}}
}
\value{
Trendy analysis result, including the fitted model parameters
and statistics for each feature.
}
\description{
Run segmented regression analysis with Trendy on imputed data
for one group of samples in a SummarizedExperiment object. For use in the
\code{run_Trendy()} function.

If less than 2 * minNumInSeg time points are available, Trendy analysis will
not be performed and NULL will be returned.
}
\keyword{internal}
