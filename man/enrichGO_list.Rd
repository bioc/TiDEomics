% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrichGO_list.R
\name{enrichGO_list}
\alias{enrichGO_list}
\title{GO enrichment with gene sets}
\usage{
enrichGO_list(
  gene_list,
  keyType = "SYMBOL",
  OrgDb,
  universe = NULL,
  universe_list = NULL,
  pAdjustMethod = "BH",
  pvalueCutoff = 0.05,
  qvalueCutoff = 0.05,
  category = NULL,
  simplify = FALSE,
  simplify_cutoff = 0.7,
  simplify_by = "p.adjust",
  simplify_select_fun = min,
  simplify_measure = "Wang",
  ...
)
}
\arguments{
\item{gene_list}{A named list of gene vectors, or a data.frame
with \code{Feature} and \code{Module} columns from \code{WGCNA_module()}.}

\item{keyType}{Available options are
\code{AnnotationDbi::keytypes(OrgDb)} (default is "SYMBOL")}

\item{OrgDb}{Organism database, e.g. org.Hs.eg.db, org.Mm.eg.db}

\item{universe}{Background genes for all input gene sets,
used if \code{universe_list} is not provided}

\item{universe_list}{Background genes for each input gene set,
a list of gene vectors with the same names as \code{gene_list}}

\item{pAdjustMethod}{Parameter of \code{clusterProfiler::enrichGO()}
(default is "BH")}

\item{pvalueCutoff}{Parameter of \code{clusterProfiler::enrichGO()}
(default is 0.05)}

\item{qvalueCutoff}{Parameter of \code{clusterProfiler::enrichGO()}
(default is 0.05)}

\item{category}{GO category to analyze (default is all three of
BP, MF, CC)}

\item{simplify}{Whether to simplify the GO terms by removing
redundant terms with \code{clusterProfiler::simplify()} function.
(default is FALSE)}

\item{simplify_cutoff}{Parameter of \code{clusterProfiler::simplify()},
cutoff for similarity when simplifying GO terms (default is 0.7)}

\item{simplify_by}{Parameter of \code{clusterProfiler::simplify()},
method to choose representative term when simplifying GO terms
(default is "p.adjust")}

\item{simplify_select_fun}{Parameter of
\code{clusterProfiler::simplify()}, function to select representative term when
simplifying GO terms (default is \code{min})}

\item{simplify_measure}{Parameter of
\code{clusterProfiler::simplify()},
method to calculate similarity when simplifying GO terms
(default is "Wang")}

\item{...}{additional arguments passed to \code{clusterProfiler::enrichGO()}}
}
\value{
A nested list of GO enrichment results with sublists:
'all' and 'simplified' (if \code{simplify = TRUE})
containing merged results of all or simplified terms across gene sets
for each GO category;
'unmerged_all' and 'unmerged_simplified' (if \code{simplify = TRUE}) including
all or simplified terms for each gene set and GO category.
}
\description{
Gene ontology enrichment analysis of multiple gene sets
with clusterProfiler, allowing for using different or same background
genes for each gene set
}
\examples{
if (requireNamespace("org.Mm.eg.db", quietly = TRUE)) {
    library(org.Mm.eg.db)
    library(clusterProfiler)
    data(example_net)
    # select two modules for demonstration
    example_module <- WGCNA_module(example_net) |>
        dplyr::filter(Module \%in\% c("1", "2"))
    # set cutoff to 1 to show all results for demonstration
    example_go_list = enrichGO_list(example_module, OrgDb = org.Mm.eg.db,
        universe = WGCNA_module(example_net, exclude_grey = FALSE)$Feature,
        pvalueCutoff = 1, qvalueCutoff = 1,
        category = "BP", simplify = FALSE)
}
}
